<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:ApiXamarin.ViewModels" x:DataType="viewmodels:IIngredientAnalysisViewModel"
             x:Class="ApiXamarin.Views.AllergenPage"
             Title="Ingredients">
    
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"/>
        <Picker ItemsSource="{Binding Ingredients}" IsVisible="true"/>
    </StackLayout>
    
</ContentPage>